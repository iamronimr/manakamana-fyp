<html lang='en'>

<head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Manakamana Online | Ecommerce Website</title>
    <script src="https://kit.fontawesome.com/dca5378fb4.js" crossorigin="anonymous"></script>
    {{!-- bootstrap css --}}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    {{!-- bootrtap js --}}
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"
        integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js"
        integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js"
        integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl"
        crossorigin="anonymous"></script>
    {{!-- axios define --}}
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

    <link href="https://unpkg.com/boxicons@2.0.7/css/boxicons.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="css/admin.css">
    <link rel="stylesheet" href="css/manage_product.css">
</head>
<style>
    .table thead.thead-primary {
        border-collapse: collapse;
        background: #a353bb;
    }

    .table thead th {
        border: none;
        padding: 30px;
        font-size: 13px;
        font-weight: 500;
        color: white;
    }

    table {
        min-width: 1000px !important;
        width: 100%;
        background: #fff;
        -webkit-box-shadow: 0px 5px 12px -12px rgba(0, 0, 0, 0.29);
        -moz-box-shadow: 0px 5px 12px -12px rgba(0, 0, 0, 0.29);
        box-shadow: 0px 5px 12px -12px rgba(0, 0, 0, 0.29);
    }

    .table tbody tr {
        margin-bottom: 10px;
    }

    img {
        height: 32px;
        width: auto;
    }

    .table tbody th,
    .table tbody td {
        border: none;
        padding: 30px;
        font-size: 14px;
        background: #fff;
        border-bottom: 4px solid #f8f9fd;
        vertical-align: middle;
    }
</style>

<body>
    <div class="sidebar">
        <div class="logo-details">
            <img src="images/logo.jpg" height="50px" width="100px">
            <!-- <i class="bx bxl-c-plus-plus"></i> -->
            <span class="logo_name">ManakamanaOnline</span>
        </div>
        <ul class="nav-links">
            <li>
                <a href="/admin">
                    <i class="bx bx-grid-alt"></i>
                    <span class="links_name">Dashboard</span>
                </a>
            </li>
            <li>
                <a href="#" class="active">
                    <i class="bx bx-box"></i>
                    <span class="links_name">Customer List</span>
                </a>
            </li>
            <li>
                <a href="#">
                    <i class="bx bx-list-ul"></i>
                    <span class="links_name">Order list</span>
                </a>
            </li>
            <li>
                <a href="#">
                    <i class="bx bx-pie-chart-alt-2"></i>
                    <span class="links_name">Service Management</span>
                </a>
            </li>
            <li>
                <a href="/manage_product">
                    <i class="bx bx-coin-stack"></i>
                    <span class="links_name">Product List</span>
                </a>
            </li>
            <li>
                <a href="/manage_worker">
                    <i class="bx bx-book-alt"></i>
                    <span class="links_name">Worker List</span>
                </a>
            </li>
            <li class="log_out">
                <a href="#" onclick="logout()">
                    <i class="bx bx-log-out"></i>
                    <span class="links_name">Log out</span>
                </a>
            </li>
        </ul>
    </div>
    {{!-- section starts --}}
    <section class="home-section">
        <nav>
            <div class="sidebar-button">
                <i class="bx bx-menu sidebarBtn"></i>
                <span class="dashboard">Dashboard</span>
            </div>
            <div class="search-box">
                <input type="text" placeholder="Search..." />
                <i class="bx bx-search"></i>
            </div>
            <div class="profile-details">
                <img src="images/logo.jpg" alt="" />
                <span class="admin_name">Admin</span>
                <i class="bx bx-chevron-down"></i>
            </div>
        </nav>
        <div class="home-content">
            <h1>Customer Data</h1>

            <table class="table">
                <thead class="thead-primary">
                    <tr>
                        <th>&nbsp;</th>
                        <th>Fullname</th>
                        <th>Description</th>
                        <th>Contact</th>
                        <th>Service Type</th>
                        <th>Address</th>
                        <th>Action</th>
                    </tr>
                </thead>                              
            </table>
        </div>        
    </section>

    <script>
        let sidebar = document.querySelector(".sidebar");
        let sidebarBtn = document.querySelector(".sidebarBtn");
        sidebarBtn.onclick = function () {
            sidebar.classList.toggle("active");
            if (sidebar.classList.contains("active")) {
                sidebarBtn.classList.replace("bx-menu", "bx-menu-alt-right");
            } else sidebarBtn.classList.replace("bx-menu-alt-right", "bx-menu");
        };


        function logout() {
            console.log('Logout')
            // Clear local storage
            localStorage.removeItem('token');
            localStorage.removeItem('role');
            window.location.href = "/";
        }


        // Fetch workers from backend API
        axios.get('http://localhost:3000/customers')
            .then(function (response) {

                console.log("This is customer" + response.data);
                // Handle successful response
                 = response.data;

                const customers = document.getElementById('data');
                userlist.forEach(function (user) {
                    const userElement = document.createElement('tr');
                    userElement.innerHTML = `
            <td><img src="products/${worker.photo}" height="250px" width="200px" /></td>
            <td>${user.username}</td>
            <td>${worker.email}</td>
            <td>${worker.description}</td>
            <td>Rs. ${worker.contact}</td>
            <td>
                <button type="button" class="btn btn-primary btn-sm"  data-toggle="modal" data-target="#editModal" onclick="editWorker('${worker.id}')">Edit</button>
                <button type="button" class="btn btn-danger btn-sm" onclick="deleteWorker('${worker.id}')">Delete</button>
            </td>
                    `;
                    console.log("Workererr" + worker.workername)
                    workersContainer.appendChild(workerElement);
                });
            })
            .catch(function (error) {
                // Handle error
                console.error('Error fetching workers:', error);
            });

        function fillEditWorkerModal(worker) {
            // Fill the input fields with worker data
            document.getElementById('fullnamename_edit').value = worker.fullname;
            document.getElementById('description_edit').value = worker.description;
            document.getElementById('contact_edit').value = worker.contact;
            document.getElementById('servicetype_edit').value = worker.servicetype;


            // Change the modal title to indicate editing
            document.getElementById('label').innerText = 'Edit Worker';

            // Change the button label to "Update"
            document.getElementById('modalfunction').innerText = 'Update';
        }

        // Function to handle edit worker action
        function editWorker(workerId) {
            console.log('Edit worker with ID:', workerId);
            const worker = workerlist.find(p => p.id === workerId);
            console.log(worker);
            fillEditWorkerModal(worker);
            document.getElementById('editWorkerId').value = workerId;
        }

        // Function to handle delete worker action
        function deleteProduct(productId) {
            // Send Delete request to backend
            axios.delete(`http://localhost:3000/workers/${workerId}`)
                .then(function (response) {
                    window.location.href = "/manage_worker"
                })
                .catch(function (error) {
                    alert(error.response.data.message);
                });
        }
    </script>
</body>

</html>